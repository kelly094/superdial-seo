# Copy this to .streamlit/secrets.toml for local use,
# or paste each value into share.streamlit.io → App settings → Secrets.
#
# The dashboard itself is read-only and doesn't need these —
# they're here for any future cloud-side pipeline features.

ANTHROPIC_API_KEY = ""
TAVILY_API_KEY = ""
WEBFLOW_API_TOKEN = ""
WEBFLOW_COLLECTION_ID = ""
